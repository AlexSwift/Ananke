-- We trust the server, no need for thread checking.

OneHalf = {}

function OneHalf.Initialize()
  
end

function OneHalf.ScreenGrab ( ply )
  
end

function OneHalf.BanPlayer ( ply )
  --Send cookie here
  --Add to mysql
  --Kick player
end

function OneHalfCheckActive ( ply )
    --Send net here
    --I will add a callback here too.
end
